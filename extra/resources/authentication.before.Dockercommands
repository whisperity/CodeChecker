#####################
# This script installs the additional "authentication" modules into a
# previously checked out CodeChecker folder:
#  - Additional system libs and executables
#
# Set up BEFORE CodeChecker is deployed in the image
#####################
#
# This is a TEMPLATE, not to be called directly.

RUN apt-get install --no-install-recommends -y \
      libldap2-dev \
      libsasl2-dev \
      libssl-dev
