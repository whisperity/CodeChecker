#####################
# This script sets up the MINIMAL CodeChecker environment:
#  - Basic required python modules
#
# Executing this step requires the CodeChecker source code to be available
# under ~/codechecker_src!
#
# Set up AFTER CodeChecker is deployed in the image
#
# This code does NOT install a runnable CodeChecker package.
#####################

RUN pip install -r ~/codechecker_src/.ci/basic_python_requirements

RUN pip install psutil